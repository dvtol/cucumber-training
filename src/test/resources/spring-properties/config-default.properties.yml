# sets boolean value for screenshots
webdriver.screenshots: true

# default remote node address
remote.url.address: http://zalenium.tools.ecom.kvknl.nl/wd/hub

# set environment of choice - SAMPLE!
target.host.name: or-notarissen-frontoffice.acp.k94.kvk.nl/zb/secureL2/kvkonlineregistreren/

# zoeken for the dev-page
dev.login: admin
dev.password: uzGcALbMUXaWJzWQ

# implicit wait timeout for element(s) search
implicit.wait.timeout.seconds: 15

# webdriver wait
timeout.interval.seconds: 15

# set local or remote browser
browser.name.local: firefox
browser.name.remote: firefox

# set local browser driver version. latest is the latest version
firefox.driver.version: latest
# e.g.: 0.19.x
chrome.driver.version: latest
# e.g.: 2.35

# directory for JSON data
test.data.dir: src/test/java/com/kvknl/regressie/data/testdata.json

# screensize
browser.screen.size: default
# e.g.: 1280x700 or default which is the maximum screensize